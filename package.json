{
  "name": "getcommits",
  "version": "0.1.0",
  "description": "GetCommits — GitHub Repository Explorer (web app)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "lint": "eslint . --ext .js,.ts",
    "format": "prettier --write \"**/*.{js,ts,css,md,json}\"",
    "test": "jest --coverage",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "keywords": [],
  "author": "GetCommits",
  "license": "MIT",
  "devDependencies": {
    "@testing-library/jest-dom": "^6.0.0",
    "cypress": "^12.0.0",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^8.0.0",
    "jest": "^29.0.0",
    "prettier": "^2.0.0",
    "vite": "^5.0.0"
  }
}